{
  "permissions": {
    "deny": [
      "Bash(dotnet ef database drop*)",
      "Bash(dotnet ef database update --startup-project * --connection *)"
    ],
    "ask": [
      "Bash(git push*)",
      "Bash(dotnet ef database update*)",
      "Bash(dotnet ef migrations remove*)"
    ]
  }
}
